# واژه‌نامهٔ دامنه — «دوکان لطیف»

نام‌هایی که در اپ، در کود، و در گفتگو **یک معنی** دارند. اگر برای مفهومی
نام تازه‌ای ساخته شد، همین‌جا نوشته شود — ورنه همان مفهوم در دو جای کود
دو نام می‌گیرد و کسی نمی‌فهمد یکی‌اند.

---

## قانون بنیادی: هیچ رقمی ذخیره نمی‌شود

قرض، موجودی صندوق، فایده — **هیچ‌کدام** در دیتابیس نگه داشته نمی‌شوند.
همه از روی **رویدادها** حساب می‌گردند. رکورد هرگز پاک نمی‌شود؛ اصلاح، یک
رویداد تازه است.

دلیلش: اگر رقم ذخیره شود، روزی با رویدادهایش جور نمی‌آید و معلوم نمی‌شود
کدام‌شان راست می‌گوید.

---

## دفترها (آرایه‌های حالت)

| نام در کود | معنی |
|---|---|
| `products` | اجناس دوکان — سه نوع: `unit` (دانه‌ای)، `weighted` (وزنی)، `wifi` (جی‌بی) |
| `sales` | فروش‌ها. `channel: "bazaar"` یعنی **فروش بازار**، نبودنش یعنی فروش دوکان |
| `purchases` | فاکتورهای خرید از تامین‌کننده |
| `customers` / `suppliers` | طرف‌حساب‌ها. `opening` = **قرض قبلی** (پیش از اپ) |
| `custPayments` | دریافت از مشتری. `kind: "goods"` یعنی **دریافت به جنس** |
| `supPayments` | پرداخت به تامین‌کننده |
| `expenses` | **مصارف خانه**. `kind: "goods"` یعنی **برداشت جنس از گدام** |
| `waste` | **ضایعات** — خراب، تاریخ‌تیر، یا کسری شمارش |
| `stockCounts` | **شمارش ماهانه** اجناس |
| `cashEntries` | رویدادهای خود صندوق: `opening` \| `in` \| `out` \| `count` |

---

## مفهوم‌های مالی

**صندوق** — پول نقدی که باید در جعبه باشد. هیچ‌جا ذخیره نمی‌شود.

**موجودی اول صندوق** (`cashEntries.kind = "opening"`) — نقطهٔ شروع دفتر
صندوق. همیشه **یکی** است؛ ثبت دوباره جای قبلی را می‌گیرد. تا ثبت نشود،
رقم صندوق پایین‌تر از واقعیت است و **محافظ صندوق** هیچ رقمی را به قرض
تبدیل نمی‌کند.

**رویدادهای صندوق** (`cashEvents()`) — **یگانه** جایی که نوشته می‌شود کدام
رکورد پول را در صندوق می‌آورد یا می‌برد. مانده، جدول تفصیل، رد پول، و
بخش صندوقِ گزارش‌ها هر چهار از همین یک فهرست ساخته می‌شوند. هر رویداد
`{date, amount, kind, label}` است؛ `amount` مثبت یعنی پول داخل شد.

**رد پول** (`cashMovements()`) — همان رویدادها، مرتب‌شده با ماندهٔ پی‌درپی.
هیچ قانونی در آن نیست؛ فقط نمایش.

**محافظ صندوق** (`guardCashOut`) — در واقعیت از جعبه‌ای که ۳٬۰۰۰ دارد
۵٬۰۰۰ پرداخته نمی‌شود. هر جایی که پول از صندوق خارج می‌شود از اینجا
می‌گذرد. `true` یعنی راه باز است؛ `false` یعنی شیت تصمیم باز شد.

**محافظ موجودی** (`guardStockOut`) — همان چیز، برای جنس: بیشتر از موجودی
گدام فروخته یا برداشته نمی‌شود.

**قیمت خرید لحظهٔ فروش** (`buyPrice` روی قلم فروش) — عکسی که در لحظهٔ ثبت
گرفته می‌شود. اگر فردا نرخ خرید عوض شود، فایدهٔ فروش‌های دیروز عوض نشود.

**اوسط وزنی** — وقتی جنسی دوباره به نرخ دیگری خریده می‌شود، قیمت خریدش
میانگین وزنی مانده و خرید تازه می‌گردد.

**باطل‌کردن** — رکورد اصلی با `reversed` نشانه می‌خورد و یک رکورد منفی با
`reverseOf` اضافه می‌شود. هیچ چیز پاک نمی‌شود.

**فروش بازار** — جنسی که در بازار (نه در دوکان) فروخته می‌شود، با نرخ
همان روز. جدا نگه داشته می‌شود تا معلوم شود کدام یکی چقدر می‌دهد.

**دریافت به جنس** — مشتری پول ندارد و به‌جایش جنس می‌دهد: قرضش کم می‌شود،
موجودی زیاد می‌شود، **صندوق دست‌نخورده** می‌ماند.

**برداشت جنس از گدام** — مصرف خانه که پول نیست، جنس است: به قیمت خرید
ارزیابی می‌شود، از موجودی کم می‌شود، **صندوق دست‌نخورده** می‌ماند.

**کسری شمارش** — فرق «آنچه باید باشد» با «آنچه شمرده شد». چه در صندوق و
چه در گدام، **ضرر واقعی** است و از فایده کم می‌شود.

---

## واژه‌های معماری

اینها در `/codebase-design` تعریف شده‌اند و در بازبینی‌های معماری همین‌طور
به‌کار می‌روند: **ماژول (module)**، **سطح (interface)**، **عمق (depth)**،
**درز (seam)**، **مجاورت (locality)**، **اهرم (leverage)**،
**آزمون حذف (deletion test)**.

`cashEvents()` نمونهٔ یک ماژول **عمیق** است: سطحش یک فنکشن بی‌پارامتر
است، ولی پشتش تمام قانون «چه چیزی پول است» ایستاده.
